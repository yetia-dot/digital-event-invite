# TODO - Admin site (create event + agenda + gift wishlist)

- [ ] Add admin landing page with navigation
- [ ] Add `/admin/events` list page (server-side fetch `events`)
- [ ] Add `/admin/events/new` create page (form for event, agenda items, wishlist items)
- [ ] Add server actions for creating the event + nested rows in Supabase
- [ ] Add `/admin/events/[eventId]` edit/summary page (optional; may redirect after create)
- [ ] Run `npm run lint` and `npm run build`
- [ ] Verify end-to-end: create event -> agenda_items + wishlist_items appear for invite pages

